import{it,expect,vi}from"vitest";import{SolicitarRotacaoCredencialService}from"./SolicitarRotacaoCredencialService";
it("cria rotação idempotente",async()=>{const tx:any={ambienteTenant:{findUnique:vi.fn().mockResolvedValue({id:"a",assinanteId:"s",status:"ATIVO",credentialVersion:1})},eventoProvisionamento:{findUnique:vi.fn(),create:vi.fn().mockResolvedValue({id:"e"})},auditLogPlataforma:{create:vi.fn()}};const db:any={$transaction:(f:any)=>f(tx)};expect(await new SolicitarRotacaoCredencialService(db).execute("a",{operadorId:"o",origem:"OPERADOR",ip:null,userAgent:null})).toMatchObject({eventoId:"e",duplicado:false});expect(tx.eventoProvisionamento.create).toHaveBeenCalledWith(expect.objectContaining({data:expect.objectContaining({tipo:"ROTACIONAR_CREDENCIAL",chaveIdempotencia:"rotacionar:a:v2"})}));});
